import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputContainer: {
    minWidth: 200,
    minHeight: 40,
    justifyContent: 'center',
    alignItems: 'center',
    borderColor: '#08949B',
    borderStyle: 'solid',
    borderRadius: 20,
    borderWidth: 2,
    padding: 12,
  },
});
