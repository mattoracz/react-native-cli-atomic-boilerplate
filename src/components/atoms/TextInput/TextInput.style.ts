import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    justifyContent: 'center',
    alignItems: 'center',
    fontWeight: '600',
    fontSize: 16,
    minWidth: 200,
    minHeight: 50,
    borderColor: '#08949B',
    borderStyle: 'solid',
    borderRadius: 20,
    borderWidth: 1,
    paddingLeft: 17,
    paddingRight: 17,
  },
});
