import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  headerContainer: {
    minWidth: 200,
    minHeight: 45,
    justifyContent: 'center',
    alignItems: 'center',
  },
  headerText: {
    color: 'black',
    fontWeight: '600',
    fontSize: 30,
  },
});
